# Pity Tracker

A lightweight, purely client-side probability calculator for gacha games using piecewise probability modeling and Monte Carlo simulation.

**Note:** There is no backend, database, or authentication in v1. Presets are defined in static configuration, and all calculations run in the browser.

## Running Locally

1. Install dependencies
2. `npm run dev`
