// lib/math/monte-carlo.ts - sampling function, isolated for its own test file
export function runMonteCarloSimulation(rateFn: (k: number) => number, trials: number = 1000): number[] {
  // TODO: run 1,000 client-side random trials
  return [];
}